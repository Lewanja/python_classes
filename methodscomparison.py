#comparing static methods and class methods
class Item:
    #static methods used to perform functions that has a relationship with class but are not unique per instance
    @staticmethod
    def is_integer(num,):

    @classmethod
    def instantiate_from_something(cls):